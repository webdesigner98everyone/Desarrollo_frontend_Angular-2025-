import { FormaateaFechaPipe } from './formaatea-fecha.pipe';

describe('FormaateaFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormaateaFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
