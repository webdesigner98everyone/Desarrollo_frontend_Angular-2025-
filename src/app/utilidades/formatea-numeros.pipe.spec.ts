import { FormateaNumerosPipe } from './formatea-numeros.pipe';

describe('FormateaNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateaNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
